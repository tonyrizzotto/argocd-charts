apiVersion: v1
kind: Service
metadata:
  name: mock-service
  labels:
    app: mock-service
spec:
  selector:
    app: mock-service
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
